import React from "react";
import { GetStaticProps } from "next";
import Layout from "../components/Layout";

const Blog = (props) => {
  return (
    <Layout>
      <h1>test</h1>
    </Layout>
  );
};

export default Blog;
